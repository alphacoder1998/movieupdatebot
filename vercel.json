{
  "name": "my-movie-trailer-bot",
  "version": "1.0.0",
  "scripts": {
    "build": "python botcode.py",
    "install": "pip install -r requirements.txt"
  },
  "dependencies": {
    "python-dotenv": "^0.19.0"
  }
}
